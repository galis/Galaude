# 复制为 .env 并填入你自己的 key（.env 已被 .gitignore 忽略，不会进版本库）
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 可选：覆盖默认模型（默认 deepseek-v4-pro）
# DEEPSEEK_MODEL=deepseek-v4-pro

# 可选：关闭常规调试日志
# DEBUG=0

# 可选：流式逐片追踪——把每个 delta chunk 原始内容全记进日志（研究协议用，很啰嗦）
# 也可在 src/config.ts 里把 traceStream 默认改成 true
# TRACE_STREAM=1
